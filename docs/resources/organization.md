---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openpanel_organization Resource - openpanel"
subcategory: ""
description: |-
  An OpenPanel Organization. The top-level tenant primitive; scopes Projects, Clients, References, and Members.
---

# openpanel_organization (Resource)

An OpenPanel Organization. The top-level tenant primitive; scopes Projects, Clients, References, and Members.

## Example Usage

```terraform
resource "openpanel_organization" "acme" {
  name     = "Acme"
  timezone = "America/Chicago"
}

output "organization_id" {
  value = openpanel_organization.acme.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable Organization name. Shown in the OpenPanel dashboard.

### Optional

- `timezone` (String) IANA timezone (e.g. `America/Chicago`). Drives the dashboard's default reporting window.

### Read-Only

- `created_at` (String)
- `id` (String) Organization ID. Slugified from the name when not specified at create-time.
- `updated_at` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import openpanel_organization.acme <organization-id>
```
